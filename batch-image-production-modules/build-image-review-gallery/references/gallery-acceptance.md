# Gallery acceptance

Check four things:

1. HTML decodes as UTF-8 and Chinese labels remain readable.
2. Every visible card points to a real local image.
3. Broken images become visibly marked rather than silently blank.
4. Ordering does not create long runs of one group beyond the configured review preference.

Default hiding is reversible presentation state. It is not deletion and must not rewrite production history.
