# Finalization contract

The delivery must distinguish:

- valid local images;
- terminally blocked jobs;
- intentionally excluded items;
- unresolved evidence conflicts.

The machine index, technical prompt archive, plain prompt edition, audit summary, and human report must agree on counts.

An output directory existing is only formal existence. Parse the files, count their records, decode Chinese text, and open at least one consumer entry before describing the delivery as usable.
