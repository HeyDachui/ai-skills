# Curation policy

Metadata detects concentration; humans decide quality.

Keep these states separate:

- `visible`: selected for the default gallery;
- `hidden`: retained but not shown by default;
- `rejected`: failed the intended quality bar;
- `needs_review`: no final human decision.

Never delete merely to satisfy a diversity quota. Record why a group was reduced and which images remain visible.
